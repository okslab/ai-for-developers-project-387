# Base URL of the API this frontend talks to.
# Default (no .env file): http://localhost:4010 — the Prism mock server.
# Point it at a real backend, e.g. http://localhost:8000, without any code changes.
VITE_API_BASE_URL=http://localhost:4010
